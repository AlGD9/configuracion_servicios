server:
  port: 7000
  cloud:
    gateway:
      routes:
      - id: serv-vuelos
      # lb:// cuando se va a acceder a través de Eureka
        uri: lb://servicio-vuelos #http://localhost:8080
        predicates:
        - Path=/svuelos/**
        filters:
        - RewritePath=/svuelos/*, /
